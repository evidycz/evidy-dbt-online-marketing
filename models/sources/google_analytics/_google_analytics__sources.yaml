version: 2

sources:
  - name: google_analytics
    database: "{{ var('ads_database', target.database) }}"
    schema: "{{ var('client_name') ~ '_google_analytics' }}"

    loader: DLT

    tables:
      - name: events
      - name: items
      - name: sources