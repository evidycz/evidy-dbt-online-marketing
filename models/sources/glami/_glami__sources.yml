version: 2

sources:
  - name: glami
    database: "{{ var('ads_database', target.database) }}"
    schema: "{{ var('client_name') ~ '_glami' }}"

    loader: DLT

    tables:
      - name: categories